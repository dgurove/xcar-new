# Чтобы PWA ощущалось приложением — что сделано 13.09.2026

Откуда брался «картон» и чем закрыто. Правило на будущее — в CLAUDE.md
(«Ощущение приложения»).

## Под пальцем
- `:active` у всего нажимаемого в тот же кадр (задержка 60 мс, чтобы старт
  прокрутки не вспыхивал); голый `:hover` — только в `@media (hover: hover)`.
- `is-pending` на нажатом до рендера (`turbo:click`), `aria-busy` на кнопке
  отправки — кольцо вместо текста. Полоса Turbo — лайм под шапкой, вне слоёв.
- Долгое нажатие по хрому без callout/выделения, `-webkit-user-drag: none`.
- Закладка заливается в момент тапа (`favorite_controller`), цель 44 px.

## Переходы
- push/pop с тенью; `data-turbo-visit-direction="none"` — без анимации:
  табы, фильтры, сортировка, поиск (`data-turbo-action="replace"`), morph-refresh.
- Табы: активный горит под пальцем, тап по активному — наверх/к корню, у
  каждого таба память экрана и прокрутки (sessionStorage).
- «‹ Раздел» в шапке на страницах объекта (`x-ui.shell back`), `back_controller`.
- Кадр карточки перетекает в галерею (`view-transition-name: hero`).
- Шапка — стекло, линия при прокрутке, заголовок собирается в центр
  (`is-scrolled` / `is-past-title`, app.js `headerState`).
- Тема кругом от кнопки; cookie `theme` на общий домен — сервер рендерит
  `<html class=dark>` и цвет полосы.
- После системного жеста «назад» (iOS) restore-визит без анимации
  (`data-native-back`).

## Ничего не мигает
- `turbo-cache-control: no-preview` (превью из кэша давало второй рендер),
  `no-cache` на формах объекта.
- Morph бережёт фокус, набранное, точки и подгрузку кадров; `card-stream` —
  `method="morph"`; тосты — постоянный узел, flash — `<template id="flash">`.
- `sizes` кадра по виду списка (строка — w320), плавное появление кадров,
  `content-visibility` у карточек, без blur на телефоне.
- Запасной `@font-face` с метриками Onest — без перескока строк.
- Скелетоны в фреймах, `turbo-frame[busy]` гаснет; чат оффера — ленивый фрейм.

## Ничего не теряется
- `@vite` с `data-turbo-track=reload`; `/sw.js` с версией из хэша сборки,
  navigation preload, precache сборки, последний экран из кэша страниц.
- 419 — обмен токена и повтор; 429 и обрыв — тост с «Повторить»
  (`net.js`); страницы ошибок в оболочке; `turbo:frame-missing`.
- Черновики форм (`draft_controller`), прокрутка после выгрузки из фона.
- «Назад» на старый снимок и возврат из фона — тихий replace-morph.
- Один EventSource на сеанс (`live.js`), lastEventID, переоткрытие после 401,
  «Нет сети / Соединение…» в шапке.
- Открытая шторка — запись в истории: свайп от края закрывает её, не страницу.
- Второй тап не создаёт дубль; POST xlsx отвечает редиректом.

## Системное
- Подтверждения — своя шторка (`confirm.js`, `Turbo.config.forms.confirm`).
- Шторки: ручка, выезд, уход, свайп вниз (`sheet.js`); клавиатура только у
  полей с `autofocus`; `--kb` — полоса действий и шторка над клавиатурой.
- Клавиатура по смыслу поля (`x-ui.field`), список справочника — popover.
- Файлы — через системный лист (`file_controller`), тап по пушу на Android —
  `Turbo.visit`.
- Строки писем и уведомлений смахиваются (`x-ui.swipe`, ответ — стрим).
- Поиск по мере ввода (`/search`, `search_controller`), недавние запросы.
- Список помнит вид и сортировку (`ListPrefs`, `users.list_prefs`).

## Своё
- Таймеры: один такт, часы сервера (`Date` у `/live/badges`), на нуле форма и
  кнопка гаснут сразу, через 3 с страница перечитывается — сервер решает по
  `bids_close_at` (с 15.09 состояния «закрыт» и `CloseBids` нет).
- Фото: превью сразу, ужатие в телефоне, `sha` исходника с клиента, Wake Lock,
  повтор тапом.
- Чат: пузырь под пальцем, «↓ N» вместо прыжка, повтор тапом.
- Лента на телефоне дотягивается сама; `Server-Timing` в ответах.

## Сделано вторым заходом по решению Дмитрия
- Хром — системным шрифтом (`--font-ui`), содержимое — Onest.
- Установленное приложение: `start_url /?app=1` → cookie `app` (`MarkInstalled`):
  без первого экрана, подвала, крошек и подсказки установки; документы — в кабинете.
- Тактильный отклик: невидимые `switch`-чекбоксы под пальцем (табы, закладка,
  чипы), `haptics()` в app.js пробрасывает change как клик по хозяину.
- Долгое нажатие по карточке — своё меню (`longpress.js`).
- Вход: ключ доступа предлагается в клавиатуре (conditional mediation).

## Третий заход
- Первые два кадра страницы и главное фото — `fetchpriority="high"`.
- `Nav::staffCounts` в кэше на 30 с со сбросом из `Publisher` (события staff/park),
  `MarkThreadRead`, `MarkChatRead`, действий стоянки.
- Соседи по списку: «предыдущее» едет вправо (`data-nav-dir`).
- Уведомление открывает объект сразу, прочитанность — маячком (`/opened`);
  чужой хост и старый `/admin` — полной загрузкой.
- Очередь действий без сети (`net.js`, формы с `data-queue`): закладка,
  прочитано; при online или следующей загрузке уходят сами.

## Не делали (нужен телефон)
Свой жест «назад» от края, если системного в standalone нет.

## Назад (13.09, вечер)

В установленном приложении браузерного «назад» нет, а десктопная шапка
`back` не показывала — на Mac/iPad из редактора предложения выхода не было.
Теперь: на десктопе квадрат «‹» слева в первом ряду шапки (30 px, как
остальные квадраты), на телефоне — «‹ Назад». Подпись всюду просто
«Назад» — так попросил владелец; имя раздела в `$back[0]` остаётся только
как адрес для `Nav::backFor`. Шелл ставит «назад» сам: `Nav::backFor` берёт самый длинный
раздел / пункт кабинета / заголовок «Работы», чей путь — собственный
префикс текущего; корни разделов, пилюли кабинета и «Сделки/Почта/Чаты» —
без «назад»; страницы настроек CRM — в глубине «Настроек». Руками —
только уточнения (машина закупки → «Закупка», этап → страховая, сделка в
кабинете → «Сделки», машины стоянки с `?yard=` → «Стоянки»).
`sheet.js` возвращает ключ `turbo` записи страницы после закрытия шторки —
«‹» после шторки снова шаг по истории, а не replace.

14.09, вечер: «на компьютере её много где нету» — квадрат в шапке был
везде, но не читался: 30 px в углу, слипшийся с полем поиска среди капсул.
Теперь на десктопе «‹» — круглая кнопка слева от заголовка экрана
(`x-ui.back`, в шелле и на трёх страницах со своим h1: закупка в CRM,
оценка, закупка на сайте); на предложении и оценке справа стоят стрелки
соседей — «‹ вверх» слева, «‹ ›» по списку справа. От 1400 px, когда у
контейнера есть поля, кнопка висит в левом поле (`.has-back` / `.back-title`),
и заголовок остаётся на линии содержимого; на ноутбуках — в потоке перед
заголовком. Квадрат из шапки убран, «назад» одно. Телефон без изменений.

14.09: владелец пролистал сто машин стрелками — и сто раз «Назад», чтобы
вернуться в закупку: каждая позиция ложилась в историю. Лечится не «Назад»
(шаг по истории нужен, чтобы вернуть список с фильтрами и прокруткой), а
соседями: стрелки `x-ui.nav-arrows`, ←/→ в `nav_controller`, «Дальше» на
экране оценки и «Назад / Вперёд» пагинации теперь `replace` — позиция
всегда одна запись, «Назад» один раз — в список. Редиректы на ту же
страницу Turbo и так заменяет.

## Полоса действий (13.09, вечер)

На телефоне плашка над таб-баром прятала низ страницы (в редакторе —
«Историю»): правило отступа для `main` проигрывало утилитам шелла. Теперь у
`main` класс `main-app` и отступы в CSS по порядку: под таб-бар, с полосой —
ещё 5.5rem; `scroll-padding-bottom` — фокус в поле не уходит под полосу.
На компьютере плашки нет: от 768 `.action-bar` — парящие кнопки в правом
нижнем углу по правому краю контейнера (тень парения, `flex: 0 0 auto`
вместо кнопки во всю ширину), под ними запас 6rem.

## Файлы в приложении

`response()->download` в установленном приложении на iPhone открывает
Quick Look во весь экран — без панели браузера закрыть его нечем. Поэтому
ссылки и формы на файлы вешаются на `file_controller`: на тач-устройстве
`window.open` в самом жесте на адрес с `inline=1` — встроенный браузер
приложения с предпросмотром, «Поделиться» и «Готово»; на компьютере
контроллер снимает действие и работает обычное скачивание. `fetch` +
`navigator.share` здесь не годится: Safari даёт лист только внутри жеста,
а он истекает, пока файл собирается.

Шеринг PDF (`share_controller`) — другой случай: файл собирается заранее,
при открытии шторки, и на «Отправить» уже лежит в памяти — жест свежий.
До готовности кнопка выключена (раньше в лист уходил один текст). Запасной
путь — «Открыть PDF» через `window.open` в жесте; если `navigator.share`
упал не по отмене, флаг в `sessionStorage` и дальше «Отправить» идёт тем же
`window.open`. Каждый сбой — тостом и маяком `/share/error` с `_token` в
теле (заголовков у `sendBeacon` нет, без токена 419 — так же починен маяк
`passkey/error`). В CRM шеринг не работал вовсе: `/offers/{n}/pdf` без
домена на хосте CRM отбивал `ResolveSurface` — теперь маршрут продублирован
в `routes/crm.php`.
