# CLAUDE.md

Документация, коммиты, комментарии и ответы — **по-русски**.

## Что это

**xcar-new** — xcar.ru заново: оптовая площадка автомобилей как веб-приложение
для телефона. Один Laravel на три хоста — три PWA одного вида: `xcar.ru`
(витрина, кабинет `/account`, вход), `crm.xcar.ru` (сотрудники: предложения,
галерея, работа — сделки · почта · чаты, закупки, настройки; пути без
`/admin`, старые `xcar.ru/admin/*` → 301), `park.xcar.ru` (стоянка).
Поверхность — `App\Support\Surface`, ставит `ResolveSurface` по хосту; чужие
маршруты на хосте — 404, вошедший не-сотрудник на CRM и стоянке — стена
`auth.foreign` (403, без шапки: в кабинет на сайт, написать, выйти). **Адреса только английские** (`/account/deals`,
`/work/mail`); транслита нет, слова «ставки» нет нигде. Старые адреса транслитом —
301 через `Support\Paths` + `RedirectLegacyPaths` (единственное место со старыми именами). **Сайт закрыт, саморегистрации нет**: новый человек
приходит только по пригласительной ссылке `/i/{code}`; прежний допуск по
заявке (`SiteWall`, `DecideAccess`, Telegram `Registration`) в коде остался,
но не используется. Без Filament и чужих админ-фреймворков — свой UI-кит.

Старый проект `../xcar.ru` — донор, не источник. Подробности по модулям и
решения владельца — `notes/*.md` (главное — `notes/moduli-podrobno-2026-09-16.md`).

## Стек и принципы

- Laravel 13, PHP 8.5, Postgres 18 (данные, очередь, сессии, кэш),
  FrankenPHP/Octane, Mercure внутри Caddy. Blade + Turbo + Stimulus
  (`resources/js/controllers/<имя>_controller.js`), никаких Alpine/Livewire/Vue.
- Изменение состояния — Action + событие; контроллеры и экраны в модели не
  пишут. Событие → Mercure → Turbo Stream, никаких `poll`. POST всегда
  отвечает редиректом. Код по областям `app/{Offers,Mail,Park,Purchases,Users,
  Cars,Media,Live,Chats,Telegram,Workflow,Notifications,Push,Storage}`, HTTP по
  поверхностям `app/Http/{Site,Cabinet,Admin,Park,Auth}`, маршруты
  `routes/{web,crm,park,auth}.php`.
- `x-ui.icon` ставит `size-6` только без своего `size-`/`w-`/`h-` в классах (иначе в собранном CSS `size-6` побеждал бы
  переданный `size-4`, и мелкие иконки в чипах раздувались до 24 px). Телефон — основное устройство: таб-бар без иконок — у раздела капсула с числом позиций (`Nav::totals`,
  кэш полминуты, серая; у текущего лаймовая одной ширины под три цифры), сверху лаймовый бейдж нового
  (`Nav::badges`), у кабинета аватар, без позиций — точка (`.tab-mark`); шторки `<dialog>` (`sheet.js`),
  safe-area, 16px в полях, системные `<select>`/`<input type=date>`.
  Проверка — глазами, с телефона (Browser pane 375 px), тесты во время
  разработки не пишем — только когда ошибка молчалива и дорога.
- Один язык вида на витрину, кабинет, CRM и стоянку: классы кита и одиннадцать
  правил — в шапке `resources/css/app.css` (лайм — глагол; тёмное — только
  хром; теней и обводок нет; скругления 24/16/12/8; заголовки 500; подписей
  под элементами нет; числа табличные, полужирным только цена и главный
  показатель; пустой блок не рисуется; фраза в интерфейсе — без точки в конце).
  **Размеров текста шесть** (правило 11, с 23.09.2026): 22/26 заголовок экрана, 17 заголовок блока
  (карточка, шторка, текущий шаг, название в окошке), 15 текст, 13 вторичное (подсказка, дата, подпись
  поля, чип, пилюля), 12 метка (тег, счётчик), 11 хром (таб-бар, значок). Шкала — одним местом в
  `@theme` (`--text-xs … --text-2xl` переопределяют тайлвиндовские, поэтому `text-sm` и прочие утилиты
  сами встают на ступени), заголовки — по уровню в `@layer base` (`.box > h2` 17, `.box-nested > h2` 15),
  разметка размера не ставит: `text-[18px]` в blade — ошибка. Вложенное не крупнее вмещающего: было
  наоборот (карточка 20 внутри шага 18 — «Фото при выдаче» аномально крупным), а правило
  `.surface-park .box > h2:first-child` не срабатывало ни по структуре, ни по слоям и снесено.
  Исключения сырых значений — цена (`text-[32px]`, она и есть главный показатель по правилу 6),
  hero витрины и юридические страницы. Поля ввода остаются 16px — ниже iOS зумит страницу, у кнопки
  такого ограничения нет (строка списка бывает кнопкой). Шапка,
  таб-бар и плашка действий — одно стекло (`--bar-alpha` + размытие, капсулы —
  тон поверх), глухих полос и теней у них нет. Тёмная тема —
  `.dark` в CSS, утилит `dark:` нет. CRM и стоянка плотнее (`surface-crm|park`).
- Оболочка: `x-ui.shell` (шапка, `h1` + `actions`, «назад» ставит сам по
  `Nav::backFor`), `x-ui.cabinet` (пилюли разделов — не липкие, одна сетка `15rem + 56rem`, без h1 и крошек, корень `/account` — профиль, «Сводки» нет; разделы — списки `.row`), `x-ui.toolbar`
  (одной строкой: поиск слева, пилюли, справа две круглые кнопки — сортировка и фильтры, обе шторками;
  на телефоне пилюли своей строкой сверху. Всё состояние — в адресе, `data-turbo-action="replace"`,
  кроме поиска: он не фильтр и адрес не меняет. **Сортировка — исходы словами**, по строке на исход
  («Сначала дешёвые», «Дольше ждут»), активный с галкой; переключателя направления нет — у каталога это
  два ключа (`price` и `-price`): стрелку вверх-вниз никто не находил), `x-offer.card` в `.cards`, страница объекта
  с правой колонкой 22rem и `x-ui.action-bar` (телефон — плашка над
  таб-баром, десктоп — парящие кнопки). Подтверждения — `data-turbo-confirm`
  (`confirm.js`), не `window.confirm`. Ощущение приложения — см.
  `notes/prilozhenie-2026-09-13.md`.
- **Три вида списка** (`?vid=grid|list|table`, `Support\ListView`, память
  в `ListPrefs` — плитки/строки, сортировка и «по сколько», таблица не запоминается; без выбора список длиннее 20 строк — таблицей,
  `ListView::pick`; страницы — `x-ui.pager` вместо `links()`, на телефоне те же (бесконечной ленты нет):
  номера, «По 24 / 48 / 96» плиткам, «По 30 / 60 / 120» строкам, таблица целиком на одной странице,
  плотным спискам без вида «По 50 / 100 / 200» (`ListView::paginate` карточным, `perPage(…, PER_ROWS)`
  остальным); тулбар `x-ui.toolbar` на телефоне в две строки — пилюли на месте h1 там, где раздел
  есть в таб-баре (`x-ui.shell :phone-heading="false"`), ниже сортировка словом и кнопки; карточка —
  сетка областей (закладка в правом верхнем углу, в плитке на кадре; под ней цена, внизу кнопка, город
  слева вровень с кнопкой — `card-star`, `card-aside`, `card-place`, `card-action` прямые дети); первый столбец таблицы — номер: № предложения, ДЛ,
  ref стоянки; заголовок «Марка, модель», слова «Машина» в шапках нет): плитки и строки — одна разметка `x-offer.card` в `.cards`;
  таблица — `x-ui.table` (плотные строки, чётные заливкой, шапка липкая,
  обводок нет) со строками `x-{offer,purchase,park}.table-row`; в CRM
  предложения, галерея, машины закупки (`crm-purchase-cars`), на стоянке —
  машины; на сайте — каталог и галерея (`x-offer.site-table-row`),
  избранное, машины закупки (`x-purchase.site-table-row`, `purchase-cars`). Переключатель `x-ui.view-switch`
  — круглая кнопка с иконкой текущего вида, меню `x-ui`-`.menu` (`menu_controller`, popover).
  **Окошко строки** (`peek_controller`, только в таблице): нажатие по строке —
  выделение и окошко с фреймом `peek` (`x-ui.peek`: лента фото `x-offer.gallery strip`
  с Viewer во весь экран, название, метки, цена справа `aside`, факты, ряд `actions`,
  тело — формы, характеристики, описание; свежая строка едет в ответе слотом `row`).
  Высота везде фиксированная (телефон — полэкрана поверх таб-бара или во весь экран
  за ручку, положение помнится в localStorage; от 1024 — панель справа от таблицы,
  липкая, во всю высоту), лишнее прокручивается внутри; стрелки у счётчика — по строкам.
  Характеристики — метками и фактами, отдельного списка нет.
  Формы внутри отвечают в окошко: `peek_controller` ставит им `data-turbo-frame="peek"`
  и заголовок `X-Peek-Back`, middleware `PeekBack` переводит редирект контроллера
  (обычный `back()`, flash, ошибки) на адрес окошка — контроллеры про окошко не знают;
  `session('peek-advance')` просит перейти к следующей строке `data-unpriced`.
  Открытое окошко — запись в истории (как шторка): «Назад» закрывает его, ссылка из
  него сначала снимает запись; `?peek=ref|first` открывает строку сразу.
  Enter — раскрыть/свернуть, ⌘Enter или двойное нажатие — на страницу, ↑/↓ — по строкам.
  Содержимое по месту: закупка CRM — оценка и выбор предложений; предложения CRM —
  +15 мин / +1 ч, состояние (`.menu`), подтверждения принять/отклонить, интерес;
  стоянка — принять, переставить, выдать, акты, заметка; сайт — подтвердить ценой
  (быстрые кнопки, комментарий, отозвать), проявить интерес / узнать цену, избранное,
  закупка — предложить / изменить / отозвать цену. Переключение строк без скачка:
  прошлое содержимое притушено (`aria-busy`), пока едет новое.
  Эндпоинты `/offers/{n}/peek` (`?gallery=1` — строка галереи), `/purchases/{n}/{car}/peek`
  на обоих хостах, `/cars/{id}/peek` на стоянке. «Сколько
  прошло» в таблицах — `Support\Ago::short` («3 ч», «2 дн»).
- **Экран — как в приложении:** одно главное действие экрана — в
  `x-ui.action-bar`, не иконкой в тулбаре; человек и группа — шапка-контакт
  `x-ui.contact` (кружок, имя-`h1`, чипы, ряд круглых действий `.acts` с
  подписью); списки людей — `.row` (аватар 44, имя, `.row-sub` чипами ≤3);
  машины внутри кабинета — строками (`cabinet/buyers/offer-row`), полная
  карточка только в ленте; выбор людей — `.row-check` с галкой справа, машин —
  `.pick-card`; пустое состояние — одна фраза; на десктопе широкой строке —
  вторая колонка, а не всё вниз.
- Факты не склеиваются через « · » — чипами `.tag`/`.chip`, человек —
  `x-ui.person`, номер — `nums`-чипом. Место (город, адрес, стоянка, «где сейчас») —
  всегда `x-ui.place` с меткой на карте перед текстом (форма — классом снаружи: tag,
  chip, mark, текст); VIN на экране — всегда `x-ui.vin-code`: нажатие копирует
  (иконка справа, тост «VIN в буфере»), скрытый — звёздочки без копирования. Одно действие у элемента — элемент и
  есть кнопка с подтверждением, не кнопка рядом. Подписей-пояснений нет:
  непонятный блок переделывается. Вторичное не крупнее основного.
- **Цена не разрывается:** разряды и ₽ — неразрывными пробелами через
  `Support\Money::nums/rub` (голого `number_format(…, ' ')` в Blade нет);
  «от → до» переносится только целыми частями, `fit_controller` ужимает шрифт
  лишь когда не влезает неразрывный кусок. `.nums` — только у цен, счётчиков и
  таймеров.
- Словарь: слова «ставка» нет; менеджер **подтверждает** предложение ценой
  («подтверждения»); покупатель **проявляет интерес**; менеджер **открывает**
  предложения покупателям («показ»); «предложение» — только ТС;
  слова «машина» в интерфейсе нет: коротко — «ТС» (таб, чипы, выбор, тосты),
  заголовок карточки — «Транспортное средство», столбец названия —
  «Марка, модель»; в текстах витрины — «автомобиль». Слова «битые» нет никогда.
- VIN — `App\Cars\Vin` + память базы `VinMemory` (память важнее таблиц; год из
  VIN не подставляется, если марка его не кодирует).

## Роли, цены, покупатели

Роли `Users\Role`: `admin`/`moderator` — сотрудники (CRM); `manager` —
внешний партнёр: подтверждает ценой, ведёт сделки, закупки и **своих
покупателей** в кабинете на сайте; `buyer` — покупатель менеджера: видит
только открытое ему, одну цену и «Проявить интерес», пишет своему менеджеру чатом по предложению; `visitor` — мёртвая.
Способности — методы `Role` явными списками (`canBid`, `canSeePurchases`,
`canSeeGallery`, `canChat`, `canShare`, `canInterest`).

**Три цены оффера:** `floor_price` закупочная (только сотрудник),
`publish_price` заявленная (пусто — равна закупочной; менеджер считает её
закупочной, `Offer::declaredPrice()`, от неё `minBid()`), `asking_price`
продажи. Кто что видит — одна дверь `Offers\PriceView::for($offer, $user)`.

**Кто что видит — одна дверь** `Offer::scopeVisibleTo(?User)` / `isVisibleTo()`:
сотрудник всё; менеджер — Open/Gallery своего круга (`managers_limited` +
`offer_managers`, по умолчанию все); покупатель — Open с показом его
менеджера (лично или через группу); гость — ничего. Через неё идут каталог,
страница, live-фрагменты, PDF, подтверждение, интерес, избранное, счётчики.

**Покупатели.** `login` (латиница; вход одним полем логин / телефон / почта),
`manager_id`, `contact_fields` (что покупатель вправе указывать — снимок с
приглашения). Приглашение `Users\Invite` (`/i/{code}`): менеджер зовёт
покупателей (многоразовая, `fields`, группа), админ — менеджера, модератора или
админа (`max_uses` 1, `expires_at`, без названия, `ManagerJoined`) или покупателя
от имени менеджера. Руками в CRM людей не заводят — только ссылкой. Закрыть доступ
уже допущенному — `DecideAccess::reject` (сессии и пуши гасятся, его ссылки выключаются,
покупатели менеджера закрываются вместе с ним), вернуть — «Отклонённые → Открыть»;
удалить (`UserController::destroy`) — только без следов (`traces`: подтверждения, сделки,
предложения, покупатели, чаты). Одна дверь `IssueInvite`, один экран `x-invites.list`
(`/account/invites` и CRM «Пользователи → Ссылки»; сам экран «Пользователи» — один на CRM `/settings/users` и сайте `/account/users`, `UserController::base()`), доступ
`Invite::manageableBy` — менеджер видит и сделанные для него админом. Новая
ссылка — первая строка списка. В кабинете `h1` нет: раздел называет пилюля.
В CRM кабинет = «Настройки» `/settings`: профиль — корень и первая пилюля, дальше Пользователи · Вендоры · Тарифы · Ящики · Шаблоны · Метки · Уведомления, все экраны настроек в `x-ui.cabinet`; `/account` на CRM → 301 `/settings`.
Пилюль у менеджера семь: Профиль · Покупатели · Сделки · Деньги · Чаты · Избранное ·
Уведомления; интерес (`/account/interest`) и приглашения — строки над списком
«Покупателей» (`Nav::link(also:)` держит пилюлю активной и даёт «назад»),
подтверждения — секции «Ждут решения» и «Не состоялись» на экране «Сделки».
`AcceptInvite` даёт `approved_at` сразу.
Группы `BuyerGroup`; показы `Offers\Showing` (ровно одно из user/group),
`ShowOffers`/`HideOffers`, покупателю одно уведомление на пачку, live —
`refresh` в `user/{id}` (тему `catalog` покупатель не слушает). Интерес
покупателя — `BuyerInterestNotice` его менеджеру. Настройки уведомлений — `Notifications\Categories` (что можно выключить по роли), `Notice::category/critical`, `User::wants/quietHours`, экран `/account/notifications/settings`, отписка из письма подписанной ссылкой. Ссылка на новый пароль —
`IssuePasswordLink` (`/password/link/{token}`, сутки, одноразовая) — менеджер
покупателю, админ кому угодно. Админ передаёт покупателя `TransferBuyer`.
Форма в шторке после успешного POST закрывается сама (`app.js`).

## Ключевые двери и договорённости

- Состояния оффера — только `Workflow\ChangeOfferState`. **Приём
  подтверждений — срок, не состояние**: `Offer::bidsOpen()`/`closed()` по
  `bids_close_at`, продление «+15 мин / +1 ч»; у закупок так же.
- Шеринг PDF — `Offers\Share` (`Subject`, `SharePdf`, `Caption`), тумблер
  `share_locked` серит «Поделиться», PDF — 422, кадры под водяным знаком
  (`Media\Watermark`, `StampPhoto`/`UnstampPhoto`, `media:restamp` после
  перерисовки знака). Share отдаёт только файл — файл и текст вместе теряют файл.
- «Рекомендуем» — `offers.recommended`, чекбокс в редакторе; у названия синий кружок с галочкой
  `x-offer.recommended` (с `label` — тег с подписью: страница, окошко); фильтр — первая сужающая
  пилюля `view=recommended` (`CatalogQuery::VIEWS`) и пресет `recommended` в CRM; пилюля есть, пока
  отмеченные есть. Одно слово везде. Тулбар каталога один на все роли: что кому показывать, решают
  `CatalogQuery::allowedSorts/allowedViews`, не ветки в blade.
- Чаты — мессенджер по образцу Авито: чат — пара (оффер, участник) + вторая сторона:
  `manager_id` пуст — сотрудники площадки (CRM «Чаты»), задан — менеджер покупателя; сотрудник
  такие переписки только читает (пресет «Покупатели с менеджерами», `x-chat.box readonly`, счётчики
  не трогает). Одна дверь `Chat::isCounterpart($user)` для счётчиков, «моих» сообщений, live-тем и
  уведомлений; читать — `allows()`, писать — `canPost()`; `User::canChat()` — покупателю только с
  менеджером. Экран `/account/chats` у всех (сотруднику — и чаты площадки: другой хост в установленном
  приложении открывается во встроенном браузере, поэтому адреса уведомлений и пушей — без хоста, на CRM
  `/account/chats/{id}` → `/work/chats/{id}`) и `/work/chats` в CRM — один рисунок: панель строк
  `x-chat.row` (фото ТС с аватаром собеседника в углу, последнее, бейдж; «Администрация XCar» — `Chat::PLATFORM` — в списке всегда: обращение ещё не заведено — строка ведёт на `/account/chats/support`, первое сообщение уходит на `POST /chats/support`). Сообщение площадке без живого ответа сотрудника → `Chats\Jobs\SendAutoReply` (через 2 с «печатает», через 5 — пузырь `Actions\AutoReply`: Staff без автора, текст по времени суток, ссылка `[в настройках](/…)` — `Linkify`); один раз до ответа сотрудника, чат — `x-chat.screen`; аватар и имя в его баре ведут к человеку: менеджеру — `/account/buyers/{id}?chat=` («‹ Чат»), сотруднику — карточка `/settings/users/{id}?chat=` (`Admin\UserController::show`, любому сотруднику: контакт, чаты, сделки, интерес, админу шит `x-admin.user-sheet`), покупателю и пишущим площадке — шторка `x-chat.contact`; статус менеджера и площадки покупателю — не `seen_at`, а `Chats\Hours::presence` (9–21 МСК «в сети», иначе «был в сети N ч N мин назад» от 21:00)
  (бар собеседника с «в сети» по `users.seen_at`/`TouchSeen`, полоска ТС, лента, бар поля); на
  телефоне экран чата — экран приложения: шапка, таб-бар, подвал и пилюли скрыты через
  `body:has(.chat-page)`, свой «‹» в баре; от 1024 — панель «список | фрейм `chat-screen`»
  (`split_controller`). «Написать» со страницы ТС — `/account/chats/offer/{n}` (чат есть →
  редирект, нет — экран с пустой лентой, первое сообщение заводит чат и подменяет адрес); на
  десктопе та же кнопка открывает шторку (`emit` с `wide`). Лента `chat/messages` +
  `chat/message`: дни, склейка подряд идущих, «Новые сообщения», подгрузка старых `?before=`,
  галочки по `read_seq_user/staff` (live `chat-read`), ответ `reply_to`, правка/удаление
  (`EditMessage`/`DeleteMessage`, текст остаётся — сотрудник видит; live `chat-edit` перечитывает
  один пузырь `GET /chats/{id}/messages/{seq}`), «печатает…» — `POST …/typing` только в хаб,
  текст — через `Support\Linkify` (ссылки, телефоны, «№ N»), фото — превью до отправки, вставка из
  буфера, Viewer. Уведомления: пуш на каждое сообщение (`ChatNotice` tag `chat-{id}`, в открытый
  чат не шлётся — `Chats\Presence`), в ленту и на почту — только первое непрочитанное; тост — с
  live-события `chat` (`from`, `text`, `href`), если лента не на экране; значок приложения —
  `User::badgeCount()` (уведомления + чаты, meta `badge-count` обновляет `/live/badges`).
- **Вендоры** (`app/Vendors`, с 19.09.2026) — одним справочником вместо `insurers` + `park_clients`;
  типы `Kind` (с 25.09): страховая, лизинговая, корпоративный парк, физлицо (контрагент-человек в
  `Party::forVendor`), собственный транспорт (`billable()` — нет, счетов и писем не бывает), банк;
  тип правится и на стоянке (`/clients`). Реквизиты, договор, условия
  (формат сделки, вознаграждение, срок оплаты, НДС, «молчание = покупка»),
  контакты по ролям (`vendor_contacts`), адреса и домены отправителей
  (`senders`) и шаблон разбора (`parser`) — карточка `/settings/vendors/{id}`
  с пилюлями Обзор · Маршруты · Контакты · Тарифы; на стоянке `/clients` — те же
  вендоры, правится только стояночное. Одна дверь `Vendor::forSender(email)`
  (точный адрес важнее домена) — константы доменов в разборе нет.
  **Прайс** `park_tariffs` (`Vendors\Tariff`): базовый без вендора,
  договорной с вендором; площадка, категория ТС (`Cars\Category`), услуга
  (`TariffService`), ступени по суткам (`from_day`, бесплатные дни — ступень с 0),
  даты действия; `Tariff::ladder` берёт самый конкретный уровень целиком
  (вендор > площадка > категория), `ladderLabel` — чип «300 ₽/сут с 4 дн».
  Кто платит хранение — правило вендора (`storage_payer`,
  `buyer_storage_after_days`), не прайс. Слово в интерфейсе — «Вендор».
- **Стоянка как процесс** (`app/Park`, с 20.09.2026): положение ТС —
  `VehicleState` `expected → in_transit → stored → released` плюс `cancelled`
  («Не привезена»); фазы работы — у заявки (`RequestState` `new → scheduled →
  in_progress → done|cancelled`, лестница целиком только у эвакуации).
  Эвакуация — заявка `tow` с полями (откуда, куда, перевозчик, км, стоимость из
  прайса, страхователь): `ScheduleTow` → `StartTow` (ТС «в пути», площадка
  освобождается) → `Intake`. Осмотр — `park_inspections` (`kind
  intake|release|pickup`: пробег, топливо восьмыми, ключи, документы,
  комплектность, «требует ремонта» кузов/двигатель/ходовая, повреждения по акту и
  при перевозке, нет деталей, замена агрегатов, кто сдал и **подпись пальцем**
  — `x-park.signature`, `signature_controller`, PNG чернилами на диске `private`
  `park/signatures`, в акте над линией) — поля акта Совкомбанка;
  на ТС копия последнего; акт приёма/выдачи — `park.act` страницей и тем же
  видом в PDF (`Park\Documents\ActPdf`, dompdf через `Support\Pdf` — один на
  счета, выгрузки и акты). **Письмо вендору о принятой ТС** (`/mail/new?car=`)
  открывается с актом и фото приёма во вложениях (outbox), отправка отмечает
  бумаги «акт хранения» и «фото» отправленными этим письмом. **Три блока фото** (с 22.09.2026): у кадра
  `custom_properties` — `stage` (`App\Park\PhotoStage`: `vendor` «Фото от страховой», `intake` «Фото при приёме»,
  `release` «Фото при выдаче», `pickup`) и `source` (`mail` — приехал из письма, `app` — сняли в приложении).
  Стадию кадра из письма ставит `ImportThreadFiles` по письму вложения: письмо вендора — `vendor`, наше
  (`Message::isOurs()`) — `intake`, наш отчёт о выдаче (`Intent::Released`) — `release`; `AttachmentImporter::import`
  принимает свойства колбэком, кадры из архивов и вырезки из листов наследуют своё вложение. **Блок стоит в ленте
  того шага, на котором его снимают** (приём — чек-лист слотов `PhotoSlot`, выдача — свёрнутый блок с простым
  добавлением), на остальных шагах — карточкой справа (`x-park.photos`, `id="photos-{stage}"`, загрузка отвечает
  `park/vehicles/photos-stream` — карточки всех стадий и чек-лист слотов разом; кнопка камеры возвращается только
  той стадии, откуда пришёл запрос). Фото от страховой и фото при приёме видны на любом шаге — «а то они скрыты,
  это бред». При выдаче стирается то, что приехало из писем (`PurgeLetters` по `source=mail`, ящик вернёт),
  снятое в приложении остаётся. **Дубль наших же кадров**: в письмо уходит ужатый webp, а `sha` у media от
  исходника, поэтому `MailController::compose` пишет кадру `sent_sha` — отпечаток отправленных байтов, и
  `HasPhotos::hasFile` сверяет оба; иначе повторный импорт ветки принёс бы наши фото приёма вторым экземпляром.
  Письмо в привязанную ветку — событие `Letter` в ленте ТС и `ParkNotice::mail`
  занятому сотруднику; письма ТС — `/mail?car=`, карточка «Письма» с «Ответить». Бумаги с вендором — `park_vehicle_docs` (`DocKind`, `out|in`,
  `pending|sent|received`, скан и письмо), открываются при приёме по правилу
  вендора (`OpenDocs`), чип — сама бумага. Площадка: ряды `rows` → места
  «A-1…», `spot` у ТС, `/yards` в три вида (плитка с картой `x-park.yard-map` — ряды клетками одной ширины, занятая —
  марка и светофор простоя; строка → шторка с картой и формой; таблица с окошком `/yards/{y}/peek`), цифр-итогов нет. Тип заявки сверяется с
  состоянием (`RequestType::allowedFor`); отмена ТС — `CancelVehicle`, удаление
  только без следов (заявки, бумаги, деньги — тоже следы). **Главная стоянки — заявки** (`/` = `/requests`,
  экрана «Сегодня» нет с 26.09.2026): три вида (`x-park.request-card`, `x-park.request-row`, окошко
  `/requests/{id}/peek` = окошко ТС); **строки стоянки без кнопок** (`.surface-park .cards`: вся карточка — ссылка
  (`a.card-link`), миниатюра, название, одна строка чипов; справа `card-aside` — у заявки пилюля типа и срок, в чипах
  шаг словом (`Timeline::word`: «звонок», «в пути», «на стоянке»); у ТС дни со светофором и красный долг
  (`Ledger::debtsByVehicle`); у кандидата «Завести ›»; кнопок и слов-действий нет), пресеты Просрочено
  (красная пилюля, `tones` у `x-ui.toolbar`) · Связаться · типы ·
  «Готовые», поиск по ТС, вендор, парковка, «Мои»; **«Наличие»** `/cars` (с 30.09.2026) — только стоящие ТС,
  пилюли — парковки (`?yard=`), поиск `q` ищет по всем состояниям, `?state=` — явный фильтр для ссылок дайджеста.
  **В интерфейсе площадка — «парковка»** (раздел «Парковки», «На парковке», «Выдана с парковки»; слов «стоянка»
  и «площадка» в текстах для людей нет), в коде и адресах — `yard`, `park`.
  `Park\Today::build` — только дайджесту владельцу (`park:digest`). Доступ: своя
  площадка `users.park_yard_id` (`Park\Scope`), «только приёмка»
  `park_readonly` (`User::canManagePark`, middleware `park.manage`).
  **Дело ТС одной страницей** (с 28.09.2026): `/cars/{id}` — единственная страница ТС от письма до выдачи
  (`Park\CaseView` собирает, `park/vehicles/show`); страницы заявки нет, `/requests/{id}` → 301 на дело,
  `/requests/{id}/peek` — окошко ТС. Шапка — **только положение и люди**: `x-park.state` (состояние, место, дни),
  красный долг, «Продано», исполнитель-аватар → шторка «Я» первым, «Письма N» → окно; ничего из полей (номер,
  вендор, телефон) и из «Денег» (ставка, начислено) в шапке не повторяется — «куча тегов, в которых написано то,
  что в полях» — нейрослоп. Срок и тон открытой заявки — чипом в заголовке карточки этапа (слот `chips` у
  `x-ui.card`). **Блок «Письма»** (с 22.09.2026, `park/vehicles/letters-card`) — над таймлайном: последнее письмо ленты словами (аватар,
  кто, тег смысла, дата, `NodeTitle`, свои слова, файлы; ждущее ответа — с оранжевой точкой), кнопки «Вся цепочка N» и
  «Ответить» (`/cars/{v}/letters?reply=1` — окно с открытым редактором, `x-mail.chain :reply-open`); чипа «Письма N»
  в шапке нет. **Теги ошибок данных** в `x-park.state`, карточке и строке таблицы: «Парковка не указана» и «Писем нет»
  (жёлтые, `Vehicle::noLetters()` по `threads_count`),
  «Ошибка в VIN» / «VIN отсутствует» (красный, `Vehicle::vinProblem()`: 17 знаков, без I/O/Q, контрольная цифра у
  северо-американских). **Номера копируются**: `x-ui.copy-code` (номер убытка, госномер, VIN — `x-ui.vin-code` обёртка) в
  чипах дела, таблицы «Наличия», карточек и почты; `x-ui.field copy` — иконка в поле (убыток, № полиса, госномер, ПТС,
  СТС), у пустого поля её нет (`copy_controller#sync`); `.tag.nums`, `.chip.nums`, поля — выделяемый текст, хром по-прежнему `user-select: none`. «Наличие» открывается
  таблицей (`VehicleController::index`, `ListPrefs::sync(rememberTable)`).
  **Таймлайн** (`Park\Timeline::for` → `Step`, `park/vehicles/timeline` + `steps/*`): точки на линии слева, как
  трекинг посылки — Письмо › Звонок › Эвакуация › Приём › Отчёт вендору › Хранение › Выдача › Отчёт вендору;
  сделанное — строка с раскрытием (`steps/done`: чипы, фото, подпись, акт), текущее — подсказка одной строкой
  (без точек, без длинных тире) и форма прямо в линии, будущее — серым; ровно один текущий, более ранний с делом
  без формы (неотправленный отчёт) — `todo` с кнопкой. «Звонок» — телефон крупно и три исхода кнопками-радио
  («Нужен эвакуатор», «Привезёт сам», «Не дозвонился»), подпись кнопки плашки меняется по исходу
  (`reveal_controller`, `data-reveal-label`). «Отчёт вендору» — `x-mail.window-button` с черновиком, сделан по
  `ReportSent` в ленте; после отправки из окна страница перечитывается (`window_controller`). «Хранение» — дни,
  ставка, «Продано» и «Выдать». Текущий шаг — **одна форма `act-form`** вокруг таймлайна: поля ТС
  (`park/vehicles/fields-block`: карточкой сверху, пока ТС ожидается; после приёма — свёрнутой строкой;
  `x-park.vehicle-fields`, правила `Park\VehicleFields`, сохраняет `RequestController::saveVehicle`, только
  `park.manage`) + поля шага; **плашки на деле нет** — кнопка действия стоит в конце текущего шага (`#act-submit`,
  подпись по исходу), «⋯» — круглая кнопка в шапке: «Отменить заявку» (пока ТС не принята — ТС и заявка
  исчезают, письма снова в «Из писем»; у ТС в пути сначала снимается эвакуация), «Переставить», «Перегнать»,
  «Принята / Выдана по ошибке» — и всё; состояния «Не привезена» и «Снова ждём» в интерфейсе нет. Заявки — только
  приём, эвакуация, выдача (осмотр и перестановка заявками не заводятся, «+ Заявка» — приём новой ТС); пресеты
  Все · Просрочено · Нужно позвонить · типы, «Готовых» нет. Шаги называются делом: «Нужно позвонить» → «Позвонили»,
  «Нужен эвакуатор» → «Эвакуатор назначен» → «Эвакуатор выехал», «Нужно принять» → «Принята», «Нужно отправить
  отчёт» → «Отчёт отправлен», «На хранении» (подсказка про письмо о продаже; «Покупатель» — шторка, «Выдать» — главная), «Нужно выдать» → «Выдана». Карточки «Бумаги с вендором» нет
  (отправку показывает шаг), «Документы» — всегда, над «Фотографиями», строками с «+ Документ»; акты — чипами в
  шагах и в «Деньгах». Отменённые заявки — только в истории. «Фотографии» — только когда у
  этапа нет своих блоков фото; ставка, начислено, комиссия — чипами в «Деньгах». Без заявки: у ожидаемой «Принять», у стоящей «Выдать» — `POST /requests`
  заводит и показывает форму. Справа на ПК (на телефоне под формой) — фото, бумаги, документы, деньги, история.
  **Поля ТС — только тождество**: убыток, № полиса, вендор, марка, модель, год, госномер, VIN, цвет, категория,
  страхователь с телефоном, заявленная стоимость (`value`, по ней страховая считает хранение — формула Альфы ещё не
  получена); негабарит, повреждения, показания, комплектность не спрашиваются (колонки остались) — **осмотр будет
  отдельным модулем**. **ТС без парковки** (с 22.09.2026, заведена по письмам или по факту): `yard_id` null, в деле
  текущий шаг «Нужно указать парковку» (`steps/yard`, `POST /cars/{v}/yard` → `SetYard`: правит payload события приёма —
  парковка со дня приёма, не перестановка; продана — выдача важнее, шаг остаётся делом без формы), в «Наличии» пилюля
  «Без парковки» (`?yard=none`), `x-park.state` — оранжевый чип; хранение до указания — по прайсу без площадки
  (`yardTimeline` отличает «площадка неизвестна» от «в пути» флагом `transit`). Приём: площадка, место, когда, ключи, документы (`Inspection::DOCS`), фото по
  слотам, кто сдал и подпись; выдача: когда, кому, долг и «наличными», фото при выдаче свёрнутым блоком, подпись.
  **«По какому документу» и «соответствует / не соответствует» убраны** (22.09.2026, колонки `pickup_note`,
  `matches`, `mismatch_note`, `refused` снесены): документ получателя мы не храним, а «если выдали, значит
  соответствует». Рядом с «Выдать» — **«Покупатель отказался»** (`RefuseRelease`, шторка с причиной,
  `POST /requests/{req}/refuse`): продажа снимается целиком (`MarkSold::clear` — `sold_at` и покупатель в null,
  заявка на выдачу отменена), дни снова считаются страховой по обычной ставке (покупатель платит не с продажи, а
  после льготных дней вендора, так что отказ обычно попадает в льготный срок), ТС остаётся стоять и ждёт нового
  покупателя из письма; в таймлайне между «Хранением» и «Выдачей» — красный шаг «Покупатель отказался» с причиной
  (событие `ReleaseRefused`, по шагу на каждый отказ), следом открывается черновик письма вендору «Отказ от
  получения». Акта осмотра при отказе больше нет. **Разбор письма** — `/requests/new?candidate=`
  (переделан 22.09.2026): заголовок — сама ТС (`Candidate::title()` — марка с моделью, а без них так и написано
  **«Марка не распознана»**: номер убытка и госномер идут чипами рядом, заглушка приглушена в плитке и таблице),
  под ним теги, как в почте (пересказа письма своими словами нет)
  (`park/requests/letter-tags`: «Ждёт ответа», что просят или этап, срок ответа, вендор, убыток, госномер, VIN,
  телефон ссылкой `tel:`, оранжевое «Марки и VIN в письме нет»); от 1024 две колонки (`1fr` и `28rem`) — слева
  лента писем прямо на странице (`x-mail.chain fold`: раскрыты письмо-заявка и то, что ждёт ответа, прочие
  подряд — узел «Ещё N писем»; «Ответить» тут же) и рамка скана заявки (`park/requests/scan-card`, `<iframe>`
  с PDF — рисует браузер, `max-h-[70vh]`, файл показывается только если уже на диске, `Attachment::isOnDisk`),
  справа липкие поля дела со своей прокруткой. **Плашки на этом экране нет** — кнопка и «Не заявка» стоят в конце
  правой колонки, как в деле ТС. Кнопка называет исход: «Завести заявку на приём» / «Завести стоящей» / «Завести
  и выдать»; у уже заведённой ТС — «Добавить заявку на приём», только пока приём ей вообще разрешён
  (`RequestType::allowedFor`), иначе «Привязать письма к ТС», а вместо пустых полей в правой колонке стоит сама
  ТС. Привязка проданной цепочки зовёт `MarkSold` — иначе ТС осталась бы стоять без заявки на выдачу. Тема письма
  в заметку не подставляется; парковка в «По письмам» стоит, только когда её знает адрес отправителя, иначе пусто
  («Без парковки» — штатное состояние).
  `Park\Actions\PromoteCandidate::attach` помечает кандидата, переносит кадры и привязывает ветки;
  `::forVehicle` подхватывает **ждущие** цепочки (архив «Не заявка» — решение человека, его не трогаем) при
  заведении ТС руками и при правке её номера, VIN и госномера, и только у живой ТС (у выданной письмо о той же
  машине — новый заезд) — в «Из писем» остаются только незаведённые (письма о заведённой ТС цепочку и не
  начинают, `ChainBuilder::known`). Новая заявка на приём сначала ждёт звонка (`Request::needsCall`, пресет `call`): `Contact` —
  эвакуатор (заявка становится `tow`), привезёт сам, не дозвонились (`next_call_at`, напоминание `RequestCall`).
  Главная кнопка везде — `Request::verb()`. После «Принять» и «Выдать» — дело, где шаг «Отчёт вендору» ждёт с
  черновиком (`Vehicle::reportUrl`: шаблоны `Template::park('intake'|'release'|'refusal')` или свои у вендора,
  `?act=`; после отказа от получения — окно сразу, `RequestController::withReport` → `?window=`), отправка помечает
  бумаги и пишет `ReportSent`. Письмо страховой
  «продано, заберёт такой-то» в ветку ТС на стоянке — `ParkExtractor::soldNotice` → `MarkSold`: `sold_at`,
  `pickup_name/phone/note`, заявка на выдачу, `ParkNotice::sold` и Telegram `ParkSold`; руками — шторка
  «Продано» на ТС. Выдача: «соответствует / не соответствует» (`park_inspections.matches`,
  `mismatch_note`; поле формы `fits` — `matches` перекрыло бы `form.matches()`), «не забрал» —
  `RefuseRelease`: осмотр с `refused`, ТС остаётся, акт с отказом и редактор письма.
  **Обратимость** (с 27.09.2026): «Не заявка» у кандидата везде (`reject`, тумблер `new ↔ rejected`, парсер строже:
  VIN/госномер без слов о вывозе и не от вендора — не заявка); «Заведена по ошибке» — `UnwindVehicle` (ТС `expected`
  без бумаг и денег: ветки отвязаны, кадры и кандидат назад в «Ждут», ТС и заявки стёрты; заменяет «удалить без следов»);
  «Снова ждём» — `RestoreVehicle` (`cancelled → expected` + заявка); «Принята по ошибке» — `UndoIntake` (пока хранение не
  выставлено: осмотр стёрт, бумаги `pending` сняты, `owed` аннулирован, заявка снова открыта); «Выдана по ошибке» —
  `UndoRelease` (пока счета при выдаче не оплачены). Крестик на заявке — шторка: закрыть с причиной / «Не привезут» /
  «Заведена по ошибке»; отмена выехавшей эвакуации возвращает ТС из «в пути». Выдача — только через заявку с осмотром
  (быстрой на ТС нет); «Не продано» снимает заявку на выдачу; второй открытой заявки одного типа не бывает; письмо о
  выданной или отменённой ТС — новый кандидат (`known`, `existing`, `autoPark` смотрят только живые); ветка после
  «Отвязать» (`unlinked_at`) автопривязке не поддаётся; даты приёма/выдачи правятся в «Договоре», пока хранение не выставлено.
  **Правда о положении ТС — у стоянки**: события `Park\Events\*` → слушатель
  `SyncOffer` двигает `car_place` (`Workflow\Actions\SetCarPlace`) и этап
  маршрута вывоза (`Workflow::stageForPlace`); запуск маршрута вывоза (кнопкой
  или с каждым предложением) заводит ТС и заявку `RequestTowFromOffer`; связь —
  `park_vehicles.offer_id` (`LinkOffer` по убытку или VIN). Уведомления —
  категория `park` (`ParkNotice`: письмо, назначение, срок, простой), Telegram
  `ParkLetter`, `ParkSold` и `ParkDigest` (`park:digest` 9:00 по будням), `park:tick` каждые
  5 минут. Уведомления письмом сотрудникам — `MailboxChannel` через SMTP ящика стоянки
  (`xcar.notify_account`, иначе первый ящик стоянки), системный мейлер не нужен.
- **Деньги** (`app/Billing`, с 21.09.2026). Контрагент — `billing_parties`
  (юрлицо с реквизитами или физлицо с паспортом; одна строка `is_self` — ПРАЙМ;
  `Party::forVendor/forUser` заводят по первому счёту, чтение — `create: false`,
  базу не трогает). Хранение считается **на лету** (`Accrual::storage`): сутки —
  календарный день, день приёма — первые, день выдачи тоже считается; `accepted_at`
  — первый приём, перегон между площадками его не сдвигает, дни в пути не
  считаются, ставка — по площадке того дня (`Vehicle::yardTimeline` из ленты:
  `yard_id` и `day` в payload приёма/перестановки/погрузки); ставка на каждый
  день из лестницы прайса (прайс, заведённый позже, действует на прошлые
  невыставленные дни), персональная `storage_rate` поверх, негабарит
  прибавляется, плательщик режет период (`storage_payer`,
  `buyer_storage_after_days` — дни за счёт вендора после продажи (`sold_at` или сделка CRM) → дальше
  покупатель (`Party::forPickup` из письма или покупатель сделки, `buyer_party_id`) по ставке вендора ×
  `buyer_rate_multiplier`, `Accrual::buyerFrom/buyerRate`). При выдаче «Принял наличными» —
  `SettleStorage`: счета по плательщикам по день выдачи, покупателю тут же оплата наличными. В счёт хранение
  идёт **по день** (`storage_until`): `IssueInvoice` считает отрезки заново под
  замком, чужой отрезок раньше своего отбивает, пропусков не бывает;
  `storage_billed_until` на ТС. Строки, добавленные в счёте, гаснут вместе с ним
  при аннулировании, начисленное до счёта — освобождается (`VoidCharge` — снять
  начисление, пока оно не в счёте: сама строка на ТС). PDF — в той же
  транзакции: счёта без файла не бывает. Счёт `billing_invoices` в обе стороны: `issued` — нам должны (номер
  сквозной в году, PDF-снимок dompdf в `file`), `owed` — мы должны (перечисление
  вендору назначенной цены по договору комиссии в `payment_days` рабочих дней —
  `IssueTransferObligation` с приёма, а также при правке договора после приёма —
  пересчёт неоплаченного, аннулирование при снятии договора); `Party::self()`
  создаёт ПРАЙМ из конфига, если строки нет; состояния `issued|paid|void`, просрочка и
  «частично» вычисляются, `light()` — светофор. Оплаты частями
  (`RecordPayment`, не больше остатка, платёжка сканом — media `slip` у
  `Payment`), у выставленного счёта правятся срок, чужой номер и заметка;
  аннулирование только без оплат (откатывает `storage_billed_until`). `Ledger` — долги по контрагенту, по ТС,
  по всем, месяц. Выдача с долгом — только с `force` или у вендора «выдавать без
  оплаты», и это в ленте. Экраны стоянки: `/money` (пресеты, **только таблица** с окошком — плиток и строк у счетов нет, PDF, акт
  хранения), карточка счёта (документы и правка — в шторке «⋯»), `/money/debts` (строки контрагентов),
  `/money/parties`; **плиток-итогов (`x-ui.stat`) на стоянке нет** — это нейрослоп, суммы — в чипах и в
  кнопке («Выставить 3 счета на 2 900 ₽», `sum_controller`);
  из ТС — `/cars/{id}/invoices/new` (хранение по день, начисления, свободные
  строки) и шторка «Начислить»; раздел `/money` — только `park.manage`, своя
  площадка видит счета своих ТС; карточка «Договор» на ТС
  (комиссия / хранение, номер, назначенная цена, ПТС, СТС, комитент). Документы
  страницей на печать: акт хранения по счёту, договор комиссии и акт
  приёма-передачи по образцу Совкомбанка (`/acts/{id}/contract|handover`,
  сумма прописью `Support\Words`). `billing:tick` 9:05 — просрочка раз, владельцу
  в Telegram с кнопкой «Оплачен» раз в неделю. У вендора в CRM — пилюля «Деньги».
  **Закрытие месяца** (`Billing\Closing`, `/money/closing?month=`): периодичность у вендора и ТС
  (`Cadence` `monthly|release`, `Vehicle::cadence()`) — помесячные по конец месяца (текущий — по сегодня),
  «после выдачи» — за весь период, покупатель отдельной строкой; экран с галочками, «Выставить выбранные»
  — `IssueInvoice` + акт PDF (`StorageActPdf`, media `act`); `billing:close-month` 1-го в 06:00 только
  считает и зовёт (Telegram `ClosingReady`, `ParkNotice::closing`). «Отправить» на счёте —
  `/mail/new?invoice=` с PDF счёта и акта бухгалтерии вендора, `sent_at`. Ничего не отправляется само.
  **Расчёты с менеджерами** (с 22.09.2026, `notes/raschety-s-menedzherami-2026-09-22.md`): при принятии
  подтверждения — шторка `x-offer.money-form` (цена, закупочная снимком в `deals.cost`, разница, **агентское
  вознаграждение** `commission` суммой, режим `commission_mode` выплачиваем / удерживает сам); менеджер
  вознаграждение не видит до первого счёта по сделке (`Deal::showsCommission`, `commissionState`). Счёт по
  сделке — `IssueDealInvoice`: «Транспортное средство» X − C и «Агентское вознаграждение» C, итого X;
  удержание — строка гасится оплатой `offset`, PDF с «Зачёт … / К оплате»; виды `sale` · `selection` (разница)
  · `reward` (вознаграждение от вендора, счёт ему) · `other`; плательщик — менеджер, его покупатель или новый.
  К выплате — `owed`-счёт `agent_fee` контрагенту менеджера (`IssueAgentFee`, 5 рабочих дней) по правилу
  `PayoutWhenPaid` (все наши счета сделки оплачены, режим выплаты); выплата — `RecordPayment`; отмена
  оплаты/счёта/сделки гасит невыплаченное (`RevokeAgentFee`). Менеджер сообщает об оплате из кабинета
  (`ClaimPayment`: `billing_payments.state=claimed`, в `paid` не входит) — `AdvanceOnClaim` делает его шаг
  «Платёжное поручение приложено»; сотрудник — `ConfirmPayment` / `RejectPayment` (CRM `/work/money`,
  пресеты заявки · к выплате · не оплачены; карточка счёта общая со стоянкой `billing/invoice-page`,
  `/work/money/invoices/{id}`; карточка «Деньги» сделки — `x-deal.money`). **Кабинет менеджера `/account/money`
  (пилюля «Деньги») — по сделкам: одна сделка — один расчёт** (`Billing\DealMoney::of` — фраза, число «что
  сейчас важно», тон, пресет; `ManagerLedger::deals/counts/position`): сверху положение строками-кнопками
  («Оплатить N», «Вам к выплате N», «Реквизиты не указаны»), пилюли Все · Оплатить · Ждут выплаты · Закрытые,
  строки `x-money.deal-row`; расчёт `/account/money/deals/{id}` — документ (цена → счёт со строками, оплатами
  и заявками → вознаграждение → выплаты → история), плашка «Сообщить об оплате»; реквизиты (`PartyKind`
  юрлицо · ИП · самозанятый · физлицо, `x-billing.party-fields`, `PartyRules`), акт сверки PDF
  (`StatementPdf`) и Excel (`ManagerStatement`) — в «···» у пилюль; отдельной страницы счёта и ленты истории
  нет (`/account/money/invoices/{id}` → расчёт); колонка узкая и на компьютере; одна дверь
  `Invoice::visibleToManager`. CRM `/work/money` — таблица `x-money.table-row` с окошком `admin/money/peek`
  (действия только там), `/work/money/managers` — сальдо по менеджерам, у менеджера в `/settings/users/{id}`
  пилюли «Обзор · Деньги» (`admin/users/money`: реквизиты, сделки, акт сверки, Excel). Уведомления `MoneyNotice`,
  Telegram `PaymentClaimed` («Поступило») и `AgentFeeDue` («Выплачено»). Закупки Carcade — без денег.
- **Машина по имени** (с 21.09.2026): заголовки кандидатов, секций почты, строк и Telegram — марка и модель
  (`Candidate::title()`: без марки — номер убытка, без него госномер; `hasCar()`), номер убытка — чипом; марка и
  модель в форме заведения обязательны (`VehicleFields::rules`). Словарь `Cars\Names` — разговорные и русские
  написания марок и моделей («хэнде крета», «митсу аутлендер», «фольц поло», «Белджи», «TENET T 7») → справочник
  (`Names::find` — первая марка в строке и слова за ней до госномера/VIN/года; марки, которых нет, заводит `CREATE`).
  `ParkExtractor` берёт машину из темы (Альфа СПб «заявка на прием ТС … Медведев Белджи Р621ВЕ126», ВСК «11 575 256
  прием ГОТС Changan CS35 Plus …», номер заказа — код), из тела (ВСК СПб «ТС готово к передаче …», таблица ВСК Москва
  «Фамилия И.О. клиента / Контактный телефон / Дата / Время / Вид услуги / Марка, модель» → `insured_*`, `planned_at`,
  `delivery` vendor|self, Совкомбанк через пересылку), из имён вложений («Альфа акт 1 чанган алсвин 8592.PDF» — источник
  `file`), год из VIN (`yearFromVin`, не у европейских S…Z), страхователь из темы, когда тела нет. Альфа Москва машину
  в тексте не пишет — она в скане заявки: `Extraction\AttachmentReader` (пока `NullAttachmentReader`, привязка в
  `AppServiceProvider`) — место для OCR или модели по API. Правила поменялись — `ReadLetter::VERSION` вверх и `mail:read`;
  харнесс по 179 письмам с прода: марка 12 → 79 из 179 (остальное — Альфа Москва без имени файла).
  Словарь `Cars\Names` пополняется из имён файлов Альфы («лехус 500д», «хундай тускон», «чанган юни к»,
  «соллерс атлант», «эволют ай про») и тем писем («Ликсианг»); четырёхзначный номер акта в конце имени —
  не часть модели (`STOP`). Скан заявки Альфы Москва — картинка от сканера Canon без текстового слоя,
  словарём его не взять: там нужен `AttachmentReader` с OCR или моделью по API.
  **Смысл письма** `Extraction\Intent` (`mail_messages.intent`: intake · sold · inspect · docs · accepted ·
  cancel_release · question · other; только свои слова письма — без цитат и подписи, у пересылки с телефона —
  пересланное; наше письмо «по принятому ТС» или с актом — accepted). **Цепочка кандидата** — этапы по всем письмам
  его веток (`Chains\ChainBuilder::fold` → `mail_candidates.stages`/`stage`: intake · stored · sold · released; «Принята» — наш ответ с
  актом, дата — его), в «Из писем» чип этапа («на парковке с 15 сен», «продана, заберёт покупатель»), форма
  «Завести» у такой цепочки — карточка **«По письмам»** с тумблерами этапов (дата приёма, парковка и место,
  покупатель) вместо «Заявки»: `RegisterFromLetters` заводит ТС сразу стоящей (событие `Accepted` с `day` и
  `yard_id` — хранение считается с даты приёма, бумаги «отправлено» нашим ответом, `MarkSold` → «Нужно выдать»),
  «Заведена по ошибке» такую откатывает (`UnwindVehicle::allowed`, payload `by_letters`). `RegisterFromLetters` — общий
  вход задним числом: кандидат и парковка необязательны, `released_at` в `$stages` выдаёт сразу (без осмотра и проверки
  долга, событие `Released` с `day`), наш ответ с актом и «подписанный АПП» пишут `ReportSent`. **Стоянка приведена к
  факту 22.09.2026** (прогон на проде прошёл) разовой `park:fact {xlsx} [--apply] [--report=]` (`Park\Console\FactCommand`: таблица владельца →
  ТС с датами из таблицы, цепочки по номеру/VIN с поправками опечаток, выданные по письмам до сентября не заведены,
  остальные цепочки закрыты, СПб-заявки и принятые оставлены; заметки в истории ТС «По таблице стоянки…» и «Проверить: …»;
  поля принятых после таблицы — из сканов заявок Альфы в письмах, прочитанных руками; отчёт xlsx рядом с файлом) — больше таблицей не пользуемся. У ветки ТС с `needs_reply_at` (одно
  правило почты и дела, см. «Почта — рабочий список дел») — шаг **«Нужно ответить»** в таймлайне, по шагу на
  каждую ждущую ветку (оранжевая точка, текст последнего входящего письма, «Ответить» окном, «Сделано» —
  `POST /cars/{v}/letters/{m}/done` пишет ветке `answered_at`), форма текущего шага при этом на месте;
  уведомление `ParkNotice::mail` — тем же заголовком. Текст письма о приёме (`Intent::excerpt`) — в шаге «Нужно позвонить».
  **Разобрать один раз, остальное производное** (с 21.09.2026): письмо читается один раз при приёме
  (`Mail\Reading\ReadLetter::apply` в `Ingest`, наш ответ — в `OnMessage::sent`): `mail_messages.parsed` (поля,
  номера-тождества, смысл, свои слова) с `parser_version`, номера — в `mail_message_keys` (индекс). Никто больше не
  вызывает `ParkExtractor` сам: ветки, цепочки, привязка к ТС берут `$message->fields()/keys()/ownText()`.
  Цепочка «Из писем» — `Mail\Chains\ChainBuilder`: `attach()` находит открытые цепочки по индексу (номера письма и
  его ветки; письмо с номером убытка к цепочке с другим номером не идёт), сливает связанные, кладёт письмо (любого
  направления) и сворачивает; `fold()` — чистая свёртка полей (первое непустое по письмам вендора по дате, марка ещё
  из наших файлов), номера, заказчика, темы, этапов, архив выданной. Цепочку начинает только письмо вендора с
  номером/VIN/госномером, не бухгалтерия и не автоответ. Рукотворное (`state`, `vehicle_id`, архив веток) свёртка
  не трогает. Правила чтения поменялись — `ReadLetter::VERSION` вверх, `mail:read` перечитывает старые письма (~70 мс
  на письмо, чанками, прерывать можно); правила группировки поменялись — `mail:rebuild` (состав открытых цепочек с
  нуля из индекса, ~2–3 минуты на 6,5 тыс. писем; `--fold` — только пересвернуть). Ремонтов `mail:relink`,
  `mail:reextract`, отцепок и слияний больше нет — всё это следствия свёртки. «Когда привезут» (`planned_at`) —
  дата и время из своих слов рядом со словами о приёме/передаче (`ParkExtractor::plannedAt`), в свёртке — по
  последнему письму с датой: наш ответ «прием назначен на 15.04 в 15:00» (смысл intake) переопределяет дату вендора.
  В темах « / » и « - » с пробелами — разделители, не часть номера (`CodeMatcher`). Парсером не закрыть: фотоотчёты
  сотрудника с темой-маркой без номера («lixiang l9») и продолжения РГС по имени машины («RE: BMW X7») — это к
  читалке на модели.
  **История ящика** (с 21.09.2026): в ящике storage@ письма с февраля 2025, `mail:backfill` забирает их
  (заголовки, текст, опись вложений; по UID снизу вверх, курсор `mail_folders.backfill_uid`, прерывать можно),
  тихо (`MessageParsed(quiet)` — без тостов, бейджей, Telegram, `LetterArrived`). Файлы на диск ложатся только у
  писем с даты **«Файлы из писем с»** у ящика (`mail_accounts.files_from`, `Message::filesFrozen()`: `PinThread`,
  `CandidateCard`, `AttachmentImporter` старое пропускают; у стоянки — 2026-09-01), остальное остаётся в ящике и
  тянется по клику; **у ящика стоянки дата снята 22.09.2026** — граница старого теперь заморозка (`frozen_at`),
  письма веток живой ТС приносят файлы в любом возрасте. Смысл `released` («выдана»: наш «подписанный АПП» с файлом «акт выдачи», «закрывающие документы
  по выданному ТС», от вендора «вчера вывез») и `billing` (отчёт-акт, счета, сверка, «акты хранения» — не кандидат,
  ключей не даёт: письмо со списком VIN привязало бы ветку к десяткам цепочек). Этап `released` **закрывает** цепочку (`CloseChain` → `state=closed`, `closed_at`), если вендор после
  не писал; `mail:archive-stale` ежедневно закрывает кандидатов без писем 60 дней (решение Дмитрия). **Закрытые цепочки
  и заморозка писем** (с 22.09.2026): `closed` — конечное состояние («Закрыта» в пресете «Архив», вернуть нельзя, новое
  письмо о той же машине — новая цепочка, `matches()` закрытые не видит); письма закрытой цепочки и веток **выданной ТС**
  замораживаются (`FreezeMessages`: `mail_messages.frozen_at`, в `parsed` остаются только `intent`, `own_text`, `keys`,
  `mail_message_keys` остаются для поиска, скачанные вложения отпускаются — `Blobs::release`, тела писем не трогаются);
  `Message::filesFrozen()` учитывает `frozen_at` — лента показывает «N файлов в ящике», клик тянет из ящика.
  `mail:read` и `mail:rebuild` замороженные письма и закрытые цепочки не проходят — пересборка идёт только по живым.
  Выдача (`Release`, `RegisterFromLetters` с `released_at`) → `PurgeLetters`: кадры `photos` со `stage=mail` и бумаги
  `papers` с `source=mail` стёрты, письма веток заморожены, кадр кандидата снят; «Выдана по ошибке» (`UndoRelease`) →
  `PurgeLetters::undo`: письма прочитаны заново (`thaw` = `ReadLetter::apply`), файлы едут из ящика (`ImportThreadFiles`).
  «В архив» руками (`rejected`) тоже замораживает, «Вернуть» и новое письмо вендора — размораживают (`fold` читает
  замороженные заново). Письмо
  сотрудника с личного ящика (адрес есть у `User`) — наше: `Message::isOurs()` в смысле, этапах и «Нужно ответить».
  `Threads::bySubject` — окно ±90 дней от даты письма, не от сегодня. **Цитата не письмо**: `QuotationStripper::forwardedBody`
  считает пересылкой только тело без своих слов перед «От:/From:» (иначе ответ вендора с цитатой нашего письма получал
  отправителем наш ящик и тему из цитаты — письмо о Джили ложилось под Фотон), `ownText` — свои слова до цитаты, парсер
  берёт поля сначала из них. Бухгалтерская тема (`Intent::billingSubject`: «Стоянка ИП Кузнецов», «отчёт-акт», «акты
  хранения», «за апрель 2026») — вся ветка billing, кроме наших писем о выдаче в ней. Письмо о нескольких машинах («выдать
  ТС А и Б») ложится в каждую цепочку по номерам из темы, не сливая их; наш ответ отдельной веткой и пересылка
  сотрудника попадают в цепочку по своим номерам; марка берётся и из имён файлов наших ответов («Альфа акт выдачи
  Mazda CX5 3309», сокращения «воях фри», «мб гле», «суб фор» — в `Cars\Names`, марка по одной модели — `MODEL_BRANDS`). Номера СОГАЗ (`SGZA…D№…`, `24-82 MT …/AOND№…`,
  `MCQ-…`) и Росгосстраха (`00\d{8}`) — в `CodeMatcher`; вендоры СОГАЗ и Росгосстрах заведены миграцией.
- Разбор писем: `Template::common` — общее для всех вендоров (срок ответа
  `answer_by`, страхователь с телефоном — по строкам: строка с номером рядом со словом «клиент», имя до или после
  номера, подпись после «С уважением» не берётся — `notes/pisma-strahovatel-2026-09-29.md`,
  признаки `Offers\Flag`, НДС, держатель, требования к документам
  `DocRequirement`, категория, ответственный по убытку из «От:»); на оффер и
  ТС стоянки ложатся при «Завести». Письмо вендору из маршрута уходит
  ответственному по убытку (`offers.contact_email`), в копии — контакты с
  `always_cc`, с ящика вендора. Вложения получают `kind` по имени файла
  (`AttachmentClassifier::kindOf`).
- Почта: письмо целиком не скачивается (`BODYSTRUCTURE`), ветка → машина
  одной дверью `LinkThread`, файлы — `ImportThreadFiles`. **Цепочка «Из писем»**
  (`/offers/from-mail`, `/requests/from-mail`): `mail_candidates` — **одна ТС, сколько бы писем о
  ней ни пришло**: тождество `key` — убыток (`Code::key`, любое написание) →
  VIN → госномер → ветка; письма — пивот `mail_candidate_messages`,
  `ExtractCandidate` сверяет все тождества письма со всеми тождествами открытых
  кандидатов (`Candidate::allIdentities`), ключ поднимается до сильнейшего,
  владельцу в Telegram — только новая ТС; письмо по уже заведённой ТС (номер,
  VIN, госномер) кандидатом не становится — ветку привяжет `LinkThread::auto`.
  «Завести» (`Park\Actions\PromoteCandidate`, `Mail\Actions\PromoteCandidate`)
  привязывает все ветки кандидата, а если ТС/предложение уже завели руками —
  не плодит второе, письма к нему.
  **«Из писем» — это почта** (с 23.09.2026): свой экран с карточками цепочек снесён (`CandidateController`,
  `mail/candidates/*`, `x-mail.candidate-card|candidate-row`, `x-park.candidate-row`, окошко, три вида,
  сортировка по сроку ответа, кадр карточки), адреса остались за `MailController::fromMail` и рисуются тем же
  списком, что почта (`admin/mail/index` + `admin/mail/list`) — «это дубликация какая-то, да и не видно других
  писем цепочки». Выборка **вшита** (`box = register`): только дела, которые надо завести, снять её нечем и на
  экране про неё не написано; **пилюль там нет**, а поиск по ходу набора, сортировка и фильтры — как в почте.
  Правило заведения одно — `Mail\Boxes::register` (цепочка `state = new` при пустом своём деле или письмо-заявка
  от вендора без тождества), его же читают пилюля «Требуют внимания» и бейдж раздела `Boxes::registerCount`
  (число **дел**: у одинокого письма-заявки цепочки нет). Кнопка «Из писем» в «Заявках» и «Предложениях»
  осталась с бейджем. Письма цепочки видны все, секцией, с лентой миниатюр в окне — поэтому своего кадра у
  цепочки больше нет. «Не заявка» — свайп дела на телефоне и кнопка в заголовке на компьютере
  (`POST {queue}/{candidate}/decline`, `MailController::decline` — тумблер «не заявка» ↔ «снова ждёт»);
  «Завести» в CRM — `POST /offers/from-mail/{c}/create` (`MailController::promote`), на стоянке — разбор письма.
  **Принятые по письмам заводятся сами** (`Park\Actions\StoreByLetters`, зовут `Mail\OnMessage` при письме и
  `park:tick` следом, разовый прогон — `park:store-by-letters [--apply]`): раз мы сами написали вендору
  «принята, отчёт отправлен», ТС стоит — цепочка становится делом стоящей ТС, датой приёма из письма, без
  человека (`RegisterFromLetters` с `$by = null`). Не заводит: цепочку из архива, цепочку, чей этап «принята»
  подтверждён письмом вендора, а не нашим (вопрос о документах приёмом не является), и цепочку без марки —
  такую разберут руками по скану. Заведённая задним числом ТС «стоит долго» уже не сообщает
  (`idle_noticed_at` ставится сразу), иначе пачка старых ТС звонила бы всем разом.
  **Парковка — у контакта вендора** (`vendor_contacts.yard_id`, `Vendors\Contact::yardFor(email)`): страховые
  ведут убытки по городам, письма питерского эксперта Альфы — о машинах в Петербурге, и ТС заводится сразу на
  его парковку (в разборе она же подставляется в «По письмам»); нет такого контакта — «Без парковки» и шаг
  «Нужно указать парковку» в деле.
  **Своих фото у цепочки нет** (кадр карточки `card` снесён 23.09.2026 вместе с плитками «Из писем»,
  миграция `…_drop_candidate_cards`): `ImportCandidateFiles` только закрепляет вложения письма на диск
  (`PinThread::message`) — все фото письма видны в окне писем лентой миниатюр
  (`/mail/attachments/{id}?thumb=1`, миниатюра в `cache/mail`), к ТС их приносит `LinkThread → ImportThreadFiles`
  при «Завести». **Сжатые версии фото (w320/w640/w960/thumb) — на диске `hot`** (SSD `/srv/xcar/hot`, Caddy
  `/hot/*`, `storeConversionsOnDisk('hot')` в `HasPhotos`, не бэкапится; `media:hot --regenerate` — разово).
  **Почта — рабочий список дел** (`MailController::screen`, переделана 23.09.2026): пилюли Требуют внимания ·
  **Все** (открывается) · Прочее · Отправленные · Архив, ветка там, где её последнее письмо (как в Gmail).
  **Пилюля выбирает дела, а не письма**: дело — это ТС (`v:`), цепочка «Из писем» (`c:`), предложение (`o:`)
  или письмо без машины (`t:`); отбирается ключ дела, берётся страница ключей, дальше грузятся **все** живые
  ветки этих дел — секции есть в каждой пилюле, и в секции стоят все письма дела, даже если под условие подошло
  одно («если требуется действие, то это для всей цепочки по этой машине»). Счётчик пилюли — число дел.
  «Требуют внимания» — три причины: цепочка в состоянии `new` (надо завести), у ветки есть `needs_reply_at`
  (надо ответить), письмо-заявка от вендора без тождества (завести руками); сортировка там по умолчанию
  «Дольше ждут». **«Ждёт ответа» — одна колонка** `mail_threads.needs_reply_at` (пишет `Threads::refresh`,
  индекс): последнее письмо ветки — их, входящее, со смыслом из `Intent::needsReply()`, и после него мы не писали
  и не нажимали «Сделано» (`answered_at`, ставит `VehicleController::letterDone`). До 23.09.2026 это считалось
  в шести местах по-разному, и числа не совпадали нигде: пилюля не пересекалась с очередью «надо завести» вовсе —
  у заявки смысл `intake`, а он в `needsReply()` не входит никогда, поэтому «завести» и «ответить» теперь в одной
  пилюле. Одно правило читают пилюля, дело ТС (`CaseView` `$asks` → шаг «Нужно ответить», по шагу на каждую
  ждущую ветку), лента (`x-mail.chain`), строка (`x-mail.thread-row`) и разбор письма.
  **Заведено или нет — видно**: у секции полоска слева (`.case--register` лаймовая — завести,
  `.case--reply` оранжевая — ответить), в заголовке слева имя дела с вендором, справа либо лаймовая «Завести»
  (`.btn-s.case-do`, 30 px — не ростом с форму), либо **само состояние ссылкой в дело** (`.case-go`:
  `x-park.state only` и «›»; слова «Дело» рядом с каждой машиной не говорили ничего). Заголовок — две части
  (`.case-head` / `.case-name`): имя с тегами переносится, действие справа стоит по середине его строк; у письма без машины заголовок есть только у заявки
  («Машину в письме не нашли» + «Завести цепочку», `POST {base}/{t}/candidate`, `ExtractCandidate::run(force)`).
  **«Прочее»** — ветки без ТС/цепочки от не-вендоров, с автоответом или с бухгалтерской темой
  (`last_intent` `auto|billing`; 17 из 52 дел «без тождества» на проде оказались перепиской бухгалтерий — это
  не дело), кнопка «Всё в архив» (`POST {base}/archive-other`). Фильтры в шторке (вендор, смысл письма,
  непрочитанные, с файлами); сортировка «Свежие» / «Дольше ждут». **Поиск по ходу набора** (с 23.09.2026,
  `live_search_controller`): поле стоит слева в тулбаре, «Написать» — в строке заголовка. Первый же знак
  сужает то, что уже на экране (`data-search-row` / `data-search-group` прячутся без сети), через 200 мс
  приходит ответ сервера и подменяет `#threads` (`admin/mail/list` по заголовку `X-List`, устаревшие ответы
  отброшены `AbortController`). **Поиск — не фильтр**: адрес он не меняет и ищет внутри текущей пилюли и
  выставленных фильтров, секции по ТС при этом остаются («поиск это просто поиск»). Полнотекст берёт
  последнее слово с префиксом (`to_tsquery('russian', 'колбас:*')`, строка разбирается в PHP — `to_tsquery`
  падает на постороннем знаке), поэтому неполное слово находится; `participants` и имена вложений — под
  триграммными GIN (`pg_trgm`), номера — под прежним GIN по `mail_threads.keys`. **Направление и смысл
  последнего письма — колонки ветки** (`mail_threads.last_direction`/`last_intent`, держит `Threads::refresh`,
  индекс `mail_threads_box_index`): коррелированные подзапросы стоили секунду на каждую пилюлю и каждый
  поиск, теперь выборка коробки — полмиллисекунды. Там же `attachments_count` — число файлов ветки:
  `withCount` через письма стоил ещё секунду, у иной ветки полторы сотни вложений.
  **Плотность почты** (с 23.09.2026): строка ветки стоила 82 px против 62 px у «Из писем» — `.row` не входила
  в плотностную систему (`.surface-crm .row, .surface-park .row` теперь такие же 8/12 px), аватар 44 → 28,
  чип ТС третьей строкой стал тегом, секции `gap-6` → `gap-4`; строка — 63 px. **Тег смысла — справа от имени**,
  а не над темой: над темой он съедал её ширину, «текст не видно вообще»; «+N» участников убрано совсем
  («раз я не понимаю, юзеры точно не поймут»). Над письмами машины — только имя, состояние и вендор
  (`x-park.state only`): номера, парковка, VIN и теги ошибок данных живут в «Наличии» и в деле, номер
  убытка и так стоит в темах писем. **Страниц у почты нет** — бесконечная лента (`endless_controller`:
  хвост с `data-endless-next`, IntersectionObserver за экран до конца, приходит тот же кусок списка);
  тот же поиск и та же лента в CRM. **Поиск в «Наличии»** — так же в тулбаре, подменяет `#cars`
  (`park/vehicles/list`, ищет по всем состояниям, выданную тоже найдёт). **Строка `x-mail.thread-row`**:
  аватар отправителя (`x-ui.avatar` с `name`/`email` — инициалы и тон по хэшу адреса; наше письмо — сотрудник или
  знак XCar), имя, тег вендора, дата; второй строкой **тег смысла** последнего входящего (`Intent::short()`/`tone()`:
  Заявка · Продано · Осмотр · Документы · Принята · Выдана · Бухгалтерия · Не вывезено · Вопрос · Не выдавать ·
  Автоответ; наше последнее — «Мы ответили», пересланное сотрудником — «От сотрудника»), тема, превью, скрепка,
  число писем; непрочитанное — полужирным с оранжевой точкой; страницы — по группам (`GROUPS_PER_PAGE`). **Номера ветки**
  `mail_threads.keys` — объединение номеров её писем из `mail_message_keys` (GIN, `Thread::withAnyKey`), письма не
  перечитываются; `LinkThread::forVehicle/forOffer` привязывает все ветки с номером ТС при «Завести», при заведении
  руками и при правке номера/VIN/госномера. **Архив один на всё** (`archived_at`, `ArchiveThread`):
  **свайп смахивает дело целиком** (`POST {base}/case/{kind}/{id}/archive`, `MailController::archiveCase`, ответ
  `admin/mail/case-stream`) — архивировать одно письмо из цепочки смысла нет; живая цепочка «Из писем» едет
  вместе с письмами: в архив — «не заявка», обратно — снова ждёт, и свёртка читает размороженные письма заново,
  а у закрытой и заведённой цепочки состояние свайп не трогает (закрытая — конечное) и работает просто архивом.
  «В архив» в окне письма осталось поветочным: окно про ту ветку, которую открыли, и это единственный способ
  убрать из дела автоответ. Новое чужое письмо возвращает ветку (`Ingest`). **Поиск по всем письмам сразу** (`mail_messages.search` tsvector russian + GIN,
  `websearch_to_tsquery`; адреса, имена файлов, номера через `Keys::fromQuery`) — список плоский, архив тоже.
  Строка почты одна на телефон и ПК (`x-mail.thread-row`: от кого / кому, тема с первыми словами, дата, «📎 N»,
  непрочитанное полужирным); таблицы и сортов у почты нет. **Письма читаются только в окне** `x-mail.window` (одно на страницу: `x-ui.sheet tall` — на телефоне во
  весь экран с «‹», от 640 окно 48rem — с `turbo-frame#letters-frame`; `window_controller` ловит `letters:open` с
  `url` от `x-mail.window-button` / строки почты (`emit` с `data-emit-url-param`); заголовок окна — ТС или
  кандидат): фреймы `/mail/{t}/window` (ветка: тема, участники, «Заявка ›» / «Завести ›»; привязка, «В архив»,
  «Не прочитано», в CRM «Страница ветки» — в меню «···»), `/cars/{v}/letters` (письма всех веток ТС одной
  лентой) и `/requests/from-mail/{c}/letters` (цепочка кандидата). **Письма в окне — лента `x-mail.chain`** (с
  22.09.2026): по времени сверху вниз на линии с точками, узел — письмо `x-mail.letter` (`<details>`): свёрнуто —
  строка «кто, заголовок, скрепка, дата» (на телефоне две строки), раскрыто — свои слова письма (`ownText`), без
  iframe; «Исходное письмо» грузит тело лениво во фрейм `body-{id}` (`GET {base}/messages/{m}/body`,
  `admin/mail/body-frame`). Точки: лаймовая — письмо этапа цепочки (`Candidate::stages`), оранжевая — письмо вендора
  с вопросом без нашего ответа, с рамкой — наше (фон у текста); автоответы и бухгалтерия подряд — один узел
  «N служебных». Тема пишется серой строкой при смене; «ч.2» того же письма (те же слова от того же адреса в сутки) — узел «Ещё
  файлы к письму» без текста; история дела — дата и время двумя строками, заметки (`Note`) реплики на подложке `history-note`. Раскрыты непрочитанные и последнее, к ним прокрутка
  (`chain_controller`). Фото письма — **лента миниатюр** (`x-mail.photo-strip`: `.photo-row`/`.photo-strip`, прокрутка пальцем,
  клик — Viewer во весь экран через `photos` readonly); чипа «N фото в галерее» со ссылкой на несуществующий
  `/cars/{id}/gallery` больше нет; у старых писем без файлов (`filesFrozen`) — «N файлов в ящике» именами. **Один «Ответить
  {кому}» внизу ленты** (`x-mail.reply-button`, фрейм `reply`, последнее письмо вендора), «Ответить на это» —
  в раскрытом письме (фрейм `reply-{id}`); «Отмена» в редакторе возвращает кнопку (`?cancel=1`). Полный вид
  письма `x-mail.message` (флажок, «Всем», «Переслать») — только на странице ветки CRM. `/mail/new?…` и
  `/mail/{t}/reply/{m}` при заголовке `Turbo-Frame` — `admin/mail/compose-frame` (форма отвечает в
  `letters-frame`, ошибки — назад в редактор по `retry`, успех — лента с ушедшим письмом внизу). `?window=` у дела и у списка
  почты открывает окно сразу; на стоянке страницы ветки нет (`/mail/{t}` → список с окном), в CRM она осталась
  («Ветка» из окна). Ссылка во фрейм внутри открытой шторки грузит фрейм, а не уводит со страницы (`app.js`).
- Закупки Carcade: витрина — две карточки на закупку (легковые/грузовые), CRM
  — один список с фильтрами в адресе, оценка — в окошке строки таблицы (`?preset=unfinal&vid=table&peek=first`,
  `POST …/{car}/estimate`; старый экран `GET …/estimate` — 301), выгрузка — тот
  самый файл Carcade (`Purchases\Export`); повторной выкачки фото нет нигде.
- Telegram-бот владельца без SDK (`Telegram\Bot`, IPv6, `telegram:poll`).
- Медиа: `PhotoIngest` — всё входящее в 1600 px webp, `sha` исходника у
  каждого кадра; вход по ключу — `laragear/webauthn` с провайдером
  `xcar-webauthn`, клиент `passkey_controller` на `@simplewebauthn/browser`.

## Команды

```bash
./serve-local.sh                 # http://xcar.localhost:8010 (php@8.5 и postgres из brew)
php artisan queue:work database-long --queue=long,mail --stop-when-empty   # почта, фото, закупки
php artisan queue:work --stop-when-empty                                   # конверсии, уведомления
php artisan offers:tick | park:tick | park:digest | billing:tick | mail:sync | mail:reconcile | push:keys | media:restamp | vendors:refill
npm run build                    # ассеты; npm run dev — с горячей перезагрузкой
node scripts/icons.mjs           # иконки, экраны запуска, водяной знак из resources/icons
./deploy/deploy.sh               # выкладка: снимок git → сборка → бэкап базы → миграции → up
./deploy/deploy.sh backup|check|artisan …|rollback TAG
```

Вход глазами без пароля — `/dev/login/{id}` (только local). Хосты локально как
в бою: `xcar.localhost`, `crm.xcar.localhost`, `park.xcar.localhost` :8010,
cookie на `.xcar.localhost`. База `xcar_new` (xcar/xcar). Почта локально —
`scripts/imapserver.py` (1143) и mailpit (1025/8025); Mercure локально нет.

## Диски

`media` (публичное, Caddy отдаёт `/media/*`), `private` (письма, документы,
чаты, чистые копии кадров — только через контроллеры), `cache`
(воспроизводимое: PDF, части писем, tmp — не бэкапится, `storage:gc`).
Что можно пересчитать — в `cache`, что нельзя — в `private` или `media`.

## Сервер

`ssh xcar-next` (root@200.169.180.17), см. `CONNECT.md`; пароли —
`CONNECT.local.md` вне git. Раскладка `/srv/xcar/{env,releases,current}`,
данные на втором диске `/srv/xcar/data/*`, бэкапы — restic в S3
(`deploy/backup.sh`). Домены `xcar.ru`, `crm.xcar.ru`, `park.xcar.ru`. Старый
сервер `xcar-new` (201.24.57.130) заморожен с 12.09.2026.
**Прод не трогать без спроса, выкладка только `deploy/deploy.sh`.**
Скомпилированные блейды — в образе (`VIEW_COMPILED_PATH=/app/bootstrap/cache/views`,
`view:cache` при сборке): том `storage/framework/views` не читается, старый
компилят из прошлого релиза всплыть не может, коммитить во время выкладки
можно.
